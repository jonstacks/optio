# Activate Flox environment if present
if command -v flox >/dev/null 2>&1; then
  use flox
fi

# Load local environment overrides if present
if [ -f .envrc-local ]; then
  source_env .envrc-local
fi

